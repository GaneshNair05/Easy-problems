#include <iostream>
#include <string>
using namespace std;
int main (){
 int breadth, length, area; //declare the variables
 cout<<"Enter the length and breadth of the rectangle";
 cin>>length>>breadth;
 area = length*breadth; //calculates area
 cout<<"The area of the rectangle is : " << area<<endl; //displays the result
 return 0;
 }
